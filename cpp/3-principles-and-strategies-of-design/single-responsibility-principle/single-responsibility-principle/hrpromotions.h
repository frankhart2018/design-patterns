#pragma once

#include "employee.h"

class HRPromotions {
public:
	bool isPromotionDueThisYear(Employee emp) {
		// Promotion logic implementation using the employee information passed in
		return true;
	}
};